#include <stdio.h>
#include<limits.h>
int main()
{ int arry[5] = {-5,4,-6,-7,-8,};
  int min=INT_MAX;
  for(int i=1;i<=4;i++){
     if(min>arry[i]){
         min=arry[i];
     }
  }
     printf("%d",min);
  
    

    return 0;
}
