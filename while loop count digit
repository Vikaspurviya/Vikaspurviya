#include <stdio.h>

int main()
{ int n;
    printf("Enter a no.:");
    scanf("%d",&n);
    int count=0;
    while(n!=0){
    n=n/10;
    count++;
    }
    printf("Count of the no.%d:",count);
    return 0;
}